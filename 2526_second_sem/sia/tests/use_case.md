which of these would be considered an extend relation
A "Print Receipt" use case conditionally extending a "Process Payment" use case only when the customer requests a receipt
A "Validate Card" use case being invoked as a mandatory step within a "Process Payment" use case
A "Customer" actor drawing a straight line to a "Place Order" use case on the diagram
A "Rent Car" use case serving as a specialized version of a "Rent Vehicle" use case

which of these would be considered a generalization relation
A "Rent Car" use case serving as a specialized version of a "Rent Vehicle" use case
A "Track Shipment" use case extending a "Place Order" use case when the customer opts for tracking
A "Make Payment" use case being included as a mandatory part of a "Complete Purchase" use case
A "Customer" actor connected by a line to a "View Menu" use case on the diagram

which of these would be considered an include relation
A "Validate Password" use case being invoked as a mandatory step within a "Login" use case
A "Customer" actor interacting with a "Place Order" use case through an association line
A "Rent Bike" use case acting as a specialized form of a "Rent Vehicle" use case
A "Generate Report" use case optionally extending a "Dashboard" use case only for admin users

which of these would be considered an association relation
A "Customer" actor drawing a straight line to a "Place Order" use case on the diagram
A "Send Notification" use case extending a "Place Order" use case when the order is confirmed
A "Calculate Tax" use case being included as a mandatory step within a "Checkout" use case
A "Rent Bike" use case acting as a specialized form of a "Rent Vehicle" use case

which of these would be considered an actor
An external payment gateway system that processes payments for the application
A "Process Refund" function that the system performs in response to a cancellation request
A rectangular boundary that encloses all the internal use cases of the system
A dashed arrow labeled "include" connecting two functional modules inside the system

which of these would be considered a use case 
A "Submit Assignment" function that the system performs when a student uploads their work
A professor who interacts with the system to view student submissions and grades
A rectangular box surrounding all campus-related functions within the diagram
A solid line with a hollow arrowhead pointing from a specialized element to a general one

which of these would be considered a system boundary
A rectangular box that encapsulates all internal use cases and separates them from external actors
An external database server that the system communicates with to store records
A "Generate Transcript" action performed by the system in response to a registrar request
A dashed arrow labeled "extend" connecting two use cases under specific conditions

of the options below, which is a benefit of use case diagrams
They provide a clear visual representation of system functionality and user interactions
They automatically generate executable code from the visual model elements
They replace the need for any written documentation or requirement specifications
They guarantee that the system will be free of bugs and security vulnerabilities
